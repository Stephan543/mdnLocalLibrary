extends layout

block content
  h1 #{title}: #{bookinstance.book}

    form(method='POST' action='')
        div.form-group
        input#bookinstanceid.form-control(type='hidden', name='bookinstanceid', required='true', value=bookimprint)

        button.btn.btn-primary(type='submit') Delete